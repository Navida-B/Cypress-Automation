describe('Finding and asserting web elements', () => {
   
    it('passes', () => {
      cy.visit('webpage1.html')
    })
    
    it('findind and asserting textbox', () => {
    //  cy.get('#text-input').type('navida').should('contain', 'navida')
    })
  })